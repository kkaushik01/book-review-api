# Initialize git
git init

# Add all project files
git add .

# Commit your code
git commit -m "Initial commit - Book Review API"

# Add the remote URL (replace YOUR_USERNAME with your GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/book-review-api.git

# Rename default branch to main
git branch -M main

# Push the code to GitHub
git push -u origin main
